language: node_js
node_js:
  "8.10.0"
script:
  "npm test"
  "npm run coverage"
services:
 -postgresql

env:
  -NODE_ENV="development"

install:
  - npm install 

notifications: 
  email:
    on_success: never
    on_failure: change